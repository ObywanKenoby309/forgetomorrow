// lib/email/sendAnalyticsEmail/js
export async function sendAnalyticsEmail({ recipients, subject, html }) {
  // placeholder — swap with Resend or SendGrid later
  console.log("EMAIL:", { recipients, subject });

  return true;
}